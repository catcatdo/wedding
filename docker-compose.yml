services:
  wedding-gift-lookup:
    build: .
    container_name: wedding-gift-lookup
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
